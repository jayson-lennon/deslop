[package]
name = "deslop-plugin-sdk"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Guest-side SDK for writing deslop WASM lint plugins"

[dependencies]
deslop-plugin-protocol = { path = "../plugin-protocol" }
serde = { workspace = true }
serde_json = { workspace = true }
