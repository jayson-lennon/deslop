# Hand-authored Tier-1 pack fragment: chatbot markup artifacts.
# See NOTICE.toml for provenance. kind = literal-ban semantics:
# CASE-INSENSITIVE substring except `{N}` = digits, `{{`/`}}` = braces.

id-base = "ARTIFACT-LIT"
kind = "literal-ban"
tier = 1
category = "grok-artifacts"
message = "Chatbot markup artifact"
scope = "anywhere"

[url]
text = "Wikipedia:Signs of AI writing"
href = "https://en.wikipedia.org/wiki/Wikipedia:Signs_of_AI_writing#Markup"

[fixtures]
must_match = [
  "grok_render_citation_card_json appeared inline.",

]

[[entries]]
slug = "grok-cards"
advice = "Remove the raw Grok render callout (`grok_render_citation_card_json`) \u2014 internal UI instructions leaked into output text"
terms = [
  'grok_render_citation_card_json',
  '{{"citation_cards"',
  '"render_citation_card"',

]
